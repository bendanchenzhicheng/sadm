<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "文档管理" %></title>

    <%= stylesheet_link_tag "application", media: "all" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="apidoc">
      <%= render "shared/top_navbar" %>

      <div class="inner row">
        <div class="large-3 columns apidoc-sidebar">
          <%= render "shared/sidebar" %>
        </div>
        <div class="large-9 columns apidoc-container">
          <%= yield %>
        </div>
      </div>

    </div>

    <%= javascript_include_tag "application" %>

  </body>
</html>
